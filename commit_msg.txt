feat(perfil): UI interativa, armazenamento local de imagens e ajustes de fluxo de autenticação

- ProfileScreen: interface interativa; campos editáveis (email, telefone, relato, senha); campos do CadÚnico (nome, CPF, RG) como somente leitura
- ProfileScreen: salvamento de imagem em base64 (data URI) via UserContext/AsyncStorage (compatível web/mobile)
- Correção do bug de rolagem (ScrollView) usando altura fixa para o container
- CadUnicoForm2Screen: persiste temporariamente dados do formulário em AsyncStorage (@Boer:tempUserData) e redireciona automaticamente para Login após cadastro
- LoginScreen: ao efetuar login, carrega @Boer:tempUserData (se existir) para o UserContext; caso contrário, chama refreshUser()
- UserContext: remoção de fallback mock, persistência via AsyncStorage, funções login/logout/updateUser/refreshUser
- Docs: inclusão de DOCS/PROFILE_AND_STORAGE_CHANGES.md com instruções de teste e notas
- Removida dependência de Firebase Storage em ambiente local por solicitação do usuário

Co-authored-by: dev-automation
